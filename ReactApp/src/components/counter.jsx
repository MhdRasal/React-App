import React from 'react'

export default function counter(obj) {
  return (
    <div>
        <p>Counter{obj.title} : {obj.count}</p>
        <p>Counter{obj.title} : {obj.count}</p>

    </div>
  )
}
